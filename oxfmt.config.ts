import { defineConfig } from "oxfmt";

export default defineConfig({
	arrowParens: "avoid",
	bracketSpacing: true,
	endOfLine: "crlf",
	insertFinalNewline: true,
	printWidth: 120,
	tabWidth: 4,
	useTabs: true,
});
